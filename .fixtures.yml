fixtures:
  repositories:
    stdlib:
      repo: "https://github.com/puppetlabs/puppetlabs-stdlib.git"
      ref: "4.14.0"
    apt:
      repo: "https://github.com/puppetlabs/puppetlabs-apt.git"
      ref: "2.2.0"
    docker:
      repo: "https://github.com/garethr/garethr-docker.git"
      ref: "v4.1.1"
  symlinks:
    gitlab_ci_runner: "#{source_dir}"
